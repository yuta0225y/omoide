
<html data-theme="dark" class="min-h-screen flex items-center justify-center bg-gray-900">
  <div class="w-full max-w-lg bg-gray-800 p-10 rounded-lg shadow-lg">
    <h1 class="text-center mb-6 text-purple-500 text-4xl font-bold">
      存在しない記憶
    </h1>
    <p class="text-center mb-8 text-gray-400 italic">
      記憶の中に潜む真実を選んでください...
    </p>
    <%= form_with url: memories_show_path, method: :get, local: true, class: "bg-gray-700 shadow-md rounded px-8 pt-6 pb-8" do %>
      <div class="mb-6">
        <%= select_tag :situation, options_for_select(@situations.map { |display, key| [display, key] }), class: "shadow appearance-none border border-gray-600 bg-gray-700 text-white rounded w-full py-3 px-4 leading-tight focus:outline-none focus:border-purple-500 focus:ring-purple-500" %>
      </div>
      <div class="flex items-center justify-between">
        <%= submit_tag '記憶を思い出す', class: "btn btn-primary bg-purple-700 hover:bg-purple-800 text-white font-bold py-3 px-6 rounded-lg transition duration-300" %>
      </div>
    <% end %>
  </div>
</html>
